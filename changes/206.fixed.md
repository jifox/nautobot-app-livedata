Fixed the `EXACT:` output filter dropping log lines that reference an interface by its full name (e.g. `Interface GigabitEthernet4/0/36`) from the Live Data interface tab.

Fixed the Read the Docs / mkdocs build failure caused by a `griffe`/`griffelib` dependency conflict (`ImportError: cannot import name 'ExprInterpolation'`) by removing the obsolete `griffe` pin and re-locking to a single griffe 2.x source.
