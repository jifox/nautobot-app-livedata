Refreshed the developer tooling: the Invoke task collection now auto-detects compose directories, parses the Nautobot version from pyproject.toml, adds djlint/djhtml/markdownlint helpers plus coverage exporters, and a new restore-database.sh script streamlines loading production backups into local environments.

Added a dedicated Nautobot test settings module and regression test that asserts the plugin configuration exposes all expected defaults, keeping future queue/alignment behavior covered by CI.
