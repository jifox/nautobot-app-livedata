Resolved breadcrumb and active tab configuration in Live Data views.

Provided a default `query_job_task_queue` plugin setting so the Livedata query APIs no longer raise HTTP 500 errors when the Celery queue name is omitted from user configuration.